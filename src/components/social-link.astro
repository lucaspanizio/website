---
import { ExternalLink } from '@lucide/astro';
import type { AstroComponent } from '@lucide/astro';

interface Props {
  title: string;
  url: string;
  username?: string;
  icon: AstroComponent;
}

const { title, url, username, icon: Icon } = Astro.props;
---

<a
  href={url}
  target="_blank"
  rel="noopener noreferrer"
  class="group inline-flex items-center gap-3 text-ink-body hover:text-accent-600 transition-colors"
>
  <div
    class="bg-surface-dim text-ink-body p-2 rounded-md group-hover:bg-accent-50 group-hover:text-accent-600 transition-all"
  >
    <Icon size={18} />
  </div>

  <div class="leading-tight">
    <p class="text-xxs sm:text-xs font-sans font-bold tracking-widest uppercase">
      {title}
    </p>
    {username && (
      <p class="text-xxs font-mono italic tracking-wider">
        {username}
      </p>
    )}
  </div>

  <ExternalLink
    size={12}
    class="opacity-0 group-hover:opacity-100 transition-opacity"
  />
</a>

